{
  "entities": {
    "CMSCollectionContent": {
      "title": "CMS Collection Content",
      "description": "Represents content or items stored inside individual CMS collections.",
      "type": "object",
      "properties": {
        "content": {
          "type": "object",
          "description": "The serialized JSON state content for this segment."
        }
      },
      "required": ["content"]
    },
    "AdminUser": {
      "title": "Admin User",
      "description": "An administrative user who has write access to the CMS.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "The email of the administrative user."
        },
        "role": {
          "type": "string",
          "description": "The role, e.g., Super Admin."
        }
      },
      "required": ["email", "role"]
    }
  },
  "firestore": {
    "/courses/{id}": {
      "schema": "CMSCollectionContent",
      "description": "Course offerings and schedule metadata."
    },
    "/faculty/{id}": {
      "schema": "CMSCollectionContent",
      "description": "Faculty directory roster data."
    },
    "/news/{id}": {
      "schema": "CMSCollectionContent",
      "description": "University news updates and press archive."
    },
    "/gallery/{id}": {
      "schema": "CMSCollectionContent",
      "description": "Image database for campus galleries."
    },
    "/settings/{id}": {
      "schema": "CMSCollectionContent",
      "description": "Administrative preferences and localized section content configuration blocks."
    },
    "/admins/{adminId}": {
      "schema": "AdminUser",
      "description": "Users authorized to log in to the admin panel."
    }
  }
}
